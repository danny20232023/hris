# Backend Environment Variables - Docker Build
# These values will be used in Docker container
# Can be overridden via docker-compose.yml environment variables

# Server Port
PORT=5000

# Public API Base URL (for CORS and URL generation)
# Default to IP address, can be overridden
API_BASE_URL=http://192.168.11.17

# Public Uploads Base URL (for generating public URLs)
# Default to IP address, can be overridden
UPLOADS_BASE_URL=http://192.168.11.17/uploads

# CORS Allowed Origins (comma-separated)
# Supports both domain names and IP addresses
# Can include both http://192.168.11.17 and IP-based URLs
CORS_ORIGINS=http://192.168.11.17,http://192.168.11.17:80

# Media Base Directory (for file storage in Docker volume)
# This should point to the mounted volume path
MEDIA_BASE_DIR=/app/uploads
